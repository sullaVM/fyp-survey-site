<div id="body-wrap">
  <div id="headings">
    <h1>
      Realistic Virtual Human Rendering in Real-Time
    </h1>
    A perceptual study on a virtual human model made with Unreal Engine.
  </div>

  <form action="/submit" method="post">

    <div id="section-one">
      <h2>
        Meet Tatiana — how realistic do you think she looks?
      </h2>
      <p class="description">
        <b>• FAKE</b> = a poor-quality generated virtual human <br>
        <b>• PHOTOREALISTIC</b> = cannot distinguish from a photograph of a real person
      </p>

      {{#each sectionOne}}
      <div class="question">
        <img src="{{this.link}}">

        <div class="options">
          <h3>{{this.label}}</h3>

          <div class="answer-wrap">
            <div class="label-wrap" style="text-align: right;">
              FAKE
            </div>

            <div class="radio-wrap">
              <input type="radio" id="age1" name="{{this.name}}" value="1">
              <input type="radio" id="age1" name="{{this.name}}" value="2">
              <input type="radio" id="age1" name="{{this.name}}" value="3">
              <input type="radio" id="age1" name="{{this.name}}" value="4">
              <input type="radio" id="age1" name="{{this.name}}" value="5">
              <input type="radio" id="age1" name="{{this.name}}" value="6">
              <input type="radio" id="age1" name="{{this.name}}" value="7">
            </div>

            <div class="label-wrap" style="text-align: left;">
              PHOTOREALISTIC
            </div>

          </div>

        </div>
      </div>
      {{/each}}

      <div style="text-align: center;">
        <span class="next-btn" onclick="goSection2()">Next</span>
      </div>
    </div>

    <div id="section-two" hidden>
      <h2>
        This is her with different rendering — how realistic does she look now?
      </h2>
      <p class="description">
        <b>• FAKE</b> = a poor-quality generated virtual human <br>
        <b>• PHOTOREALISTIC</b> = cannot distinguish from a photograph of a real person<br><br>
        Move the sliders to remind yourself what basic quality Tatiana looks like.
      </p>
      {{#each sectionTwo}}
      <div class="question-slider">
        <div class="img-slider-container">
          <div class="img-comp">
            <img src="{{this.linkA}}">
          </div>

          <div class="img-comp img-comp-top">
            <div class="clear">
              <img src="{{this.linkB}}">
            </div>
          </div>
        </div>

        <div class="options">
          <h3>{{this.label}}</h3>

          <div class="answer-wrap">
            <div class="label-wrap" style="text-align: right;">
              FAKE
            </div>

            <div class="radio-wrap">
              <input type="radio" id="age1" name="{{this.name}}" value="1">
              <input type="radio" id="age1" name="{{this.name}}" value="2">
              <input type="radio" id="age1" name="{{this.name}}" value="3">
              <input type="radio" id="age1" name="{{this.name}}" value="4">
              <input type="radio" id="age1" name="{{this.name}}" value="5">
              <input type="radio" id="age1" name="{{this.name}}" value="6">
              <input type="radio" id="age1" name="{{this.name}}" value="7">
            </div>

            <div class="label-wrap" style="text-align: left;">
              PHOTOREALISTIC
            </div>

          </div>

        </div>
      </div>
      {{/each}}

      <div style="text-align: center;">
        <span class="next-btn" onclick="goSection3()">Next</span>
      </div>

    </div>

    <div id="section-three" hidden>



      <div style="text-align: center;">
        <span class="next-btn" onclick="goSection4()">Next</span>
      </div>
    </div>

    <div id="section-four" hidden>
      <h2>
        Let's look at the details — which image do you prefer?
      </h2>
      <p class="description">
        Move the sliders to remind yourself what basic quality Tatiana looks like.
      </p>

      {{#each sectionFour}}
      <div class="question-slider">

        <div class="img-slider-container">
          <div class="img-comp">
            <img src="{{this.linkA}}">
          </div>

          <div class="img-comp img-comp-top">
            <div class="clear">
              <img src="{{this.linkB}}" class="img-top">
            </div>
          </div>
        </div>

        <div class="options">
          <h3>{{this.label}}</h3>

          <div class="answer-wrap">
            <div class="label-wrap" style="text-align: right;">
              A
            </div>

            <div class="radio-wrap">
              <input type="radio" id="age1" name="{{this.name}}" value="1">
              <span></span>
              <input type="radio" id="age1" name="{{this.name}}" value="2">
            </div>

            <div class="label-wrap" style="text-align: left;">
              B
            </div>

          </div>

        </div>

      </div>
      {{/each}}

      <div style="text-align: center;">
        <button>Submit</button>
      </div>
    </div>


  </form>

  <div style="height: 40px"> </div>

</div>